{% extends 'base.html.twig' %}

{% block body %}
    <h1>Viewtypes list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for viewType in viewTypes %}
            <tr>
                <td><a href="{{ path('get',{'applicationPath': driver.applicationPath, 'entitiesPath': driver.entitiesPath, 'id': viewType.id }) }}">{{ viewType.id }}</a></td>
                <td>{{ viewType.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('get',{'applicationPath': driver.applicationPath, 'entitiesPath': driver.entitiesPath, 'id': viewType.id }) }}">Show </a>
                        </li>
                        <li>
                            <a href="{{ path('edit',{'applicationPath': driver.applicationPath, 'entitiesPath': driver.entitiesPath, 'id': viewType.id }) }}">Edit </a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('new',{'applicationPath': driver.applicationPath, 'entitiesPath': driver.entitiesPath }) }}">new </a>
        </li>
    </ul>
{% endblock %}
 