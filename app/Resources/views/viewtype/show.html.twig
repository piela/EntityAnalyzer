{% extends 'base.html.twig' %}

{% block body %}
    <h1>Viewtype</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ viewType.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ viewType.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('viewtype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('viewtype_edit', { 'id': viewType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
