{% extends is_sub_request or is_xml_http_request ?  'blank.html.twig':'base.html.twig'  %}
{% form_theme form 'Form\\bootstrap_3_horizontal_layout.html.twig' %}
{% block body %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Edit" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('filter',{'applicationPath': driver.applicationPath, 'entitiesPath': driver.entitiesPath }) }}">List</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
