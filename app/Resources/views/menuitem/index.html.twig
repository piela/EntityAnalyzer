{% extends 'base.html.twig' %}

{% block body %}

    <style>

        ul
        {
            list-style: outside none none;
            margin: 0;
            padding: 0;

        }
        .actions {
            list-style: outside none none;
            margin: 0;
            padding: 0;
            z-index: 2;
        }
    </style>    

    <h1 class='p-l-15'>Menuitems list</h1>
    {% form_theme form 'Form\\bootstrap_3_horizontal_layout.html.twig' %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class='container'>
        <div class='row'>
            <div class='col-sm-12 text-center'>
              <button  class="btn bgm-green waves-effect btn-sm" type="submit">Szukaj</button>
              <button  class="btn bgm-green waves-effect btn-sm" type="reset" value="Reset">Reset</button>
            </div>
        </div>
    </div>
    {{ form_end(form) }}



    <ul class='p-l-15 p-t-10 p-b-10 '>
        <li>
            <a href="{{ path('new',{'application_path': driver.applicationPath, 'entities_path': driver.entitiesPath }) }}" class="btn bgm-green waves-effect btn-sm">New</a>


        </li>
    </ul>
    {{ render(controller("tm_solution_prototype.entity_controller:listAction",{ 'application_path': driver.applicationPath, 'entities_path': driver.entitiesPath,'action': 'list', 'request' : app.request })) }}
         
   


{% endblock %}
