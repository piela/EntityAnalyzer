{% extends 'base.html.twig' %}

{% block body %}

    <style>

        ul
        {
            list-style: outside none none;
            margin: 0;
            padding: 0;

        }
        .actions {
            list-style: outside none none;
            margin: 0;
            padding: 0;
            z-index: 2;
        }
    </style>    

   
    <h1 class='p-l-15'>Menuitems list</h1>
    {{ render(controller("tm_solution_prototype.entity_controller:filterAction",{ 'request' : app.request ,'application_path': 'menu', 'entities_path': 'menu-item','action': 'filter' })) }}
    {{ render(controller("tm_solution_prototype.entity_controller:listAction",{ 'request' : app.request,'application_path': 'menu', 'entities_path': 'menu-item','action': 'list' })) }}
{% endblock %}
