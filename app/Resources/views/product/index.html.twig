{% extends 'base.html.twig' %}

{% block body %}
    <h1>Products list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Datefrom</th>
                <th>Dateto</th>
                <th>Transactionname</th>
                <th>Payed</th>
                <th>Quantity</th>
                <th>Endprice</th>
                <th>Currency</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for product in products %}
            <tr>
                <td><a href="{{ path('test_show', { 'id': product.id }) }}">{{ product.id }}</a></td>
                <td>{% if product.dateFrom %}{{ product.dateFrom|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if product.dateTo %}{{ product.dateTo|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ product.transactionName }}</td>
                <td>{% if product.payed %}Yes{% else %}No{% endif %}</td>
                <td>{{ product.quantity }}</td>
                <td>{{ product.endPrice }}</td>
                <td>{{ product.currency }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('test_show', { 'id': product.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('test_edit', { 'id': product.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('test_new') }}">Create a new product</a>
        </li>
    </ul>
{% endblock %}
